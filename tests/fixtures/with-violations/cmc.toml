# With Violations - Project with intentional violations for testing
# Tests: violation detection, exit codes, output formatting

[project]
name = "with-violations"
category = "production"
description = "Test fixture with intentional code violations"

[rulesets]
default = ["default"]

[ai]
enabled = false
